# Registry deployment files

See the [isolated registry operations guide](../../docs/operations/registry.md)
for the architecture, provisioning steps, key setup, first publish, and client
smoke test. Successful CI on `main` runs the SSH deployment workflow
automatically.
